/*const subscriber200 ={
    name: "Hamphrey",
    surname: "Chinyerere",
    Age: 42,
    ownsCar: false``
}

console.log(`${name} is the 200th subscriber `)*/




const car = {
    brand: "Toyota",
    model: "starlet",
    year: 2019,
    isNew: false,
    maxTank: 25 
}

console.log(`The ${car.brand} ${car.model} has a maximum tank capacity of ${car.maxTank} liters. Made in the year ${car.year}.`)

const house = {
    model: "bungalow",
    year: 2020,
    isPainted: true,
}

console.log(`The ${house.model} was built in the year ${house.year}. Is it painted?: {}`)

const country ={
    name: "Zimbabwe",
    continent: "Africa",
    populationMillion: 15.1,
    isLandlocked: true

}

console.log(`${country.name} is a country in ${country.continent} with a population of ${country.populationMillion} million`)